Use the `tdd-guide` agent. RED: write failing test. GREEN: minimum code to pass. REFACTOR: clean up. COMMIT. No production code without a test first.

$ARGUMENTS: Optional — feature or test to focus on

Generate a design system for the project using the `design-system` agent. Recommends UI style, color palette, typography, and anti-patterns matched to the product type. Saves to `.behemoth/design-system/MASTER.md` and references it in all future frontend work.

$ARGUMENTS: Product description (e.g., "SaaS analytics dashboard for marketing teams")

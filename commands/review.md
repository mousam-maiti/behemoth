Use the `code-reviewer` agent. Stage 1: spec compliance. Stage 2: code quality (critical/major/minor/nit). Verdict: APPROVE, REQUEST CHANGES, or BLOCK.

$ARGUMENTS: Optional — file, branch, or "staged"
